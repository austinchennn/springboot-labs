# application-prod.yml — 生产环境配置（Lab 8 动手练习）
#
# TODO(Lab8): 参考 Lab8-配置管理.md 第 "方法一：多配置文件" 一节，
# 在这里配置生产环境专用的数据源、日志级别、业务开关。
# 提示：
#   - 数据库用户名密码要从环境变量读取（${DB_USERNAME} / ${DB_PASSWORD}），不要硬编码！
#   - spring.jpa.hibernate.ddl-auto 用 validate（不要自动建表）
#   - spring.jpa.show-sql 关闭
#   - logging.level.root 用 WARN
#   - app.bookshelf.cache-enabled 开启
